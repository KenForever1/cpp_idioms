
## Cpp Idioms

With the increase in the idioms a programmer understands, the language becomes friendlier to them.

## build

```bash
cd pimpl

cmake -B build -S .
cmake --build build

# excute
./build/main
```